# 複製成 values-secret-eve.yaml（已被 .gitignore，永不 commit）填入真實值。
# 只放 Discord token。OpenRouter API key 不放這裡——bootstrap 時 `opencode auth login`
# 貼與其他 opencode bot 共用的那把，存進各自 PVC 的 auth.json。
agents:
  eve:
    discord:
      botToken: "<EVE_DISCORD_BOT_TOKEN>"
